{
  "name": "latex-symbols-lsp",
  "version": "1.0.0",
  "main": "dist/server.js",
  "bin": {
    "latex-symbols-lsp": "dist/server.js"
  },
  "scripts": {
    "build": "tsc && echo '#!/usr/bin/env node' > tmp && cat dist/server.js >> tmp && mv tmp dist/server.js && chmod +x dist/server.js",
    "start": "node dist/server.js",
    "dev": "tsc --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "GPLv3",
  "description": "LSP server for LaTeX math symbol autocompletion and Unicode replacement",
  "dependencies": {
    "vscode-languageserver": "^8.1.0",
    "vscode-languageserver-textdocument": "^1.0.8"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
